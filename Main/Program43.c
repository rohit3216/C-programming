/* Program to explain the use of gets() funtion */
#include<stdio.h>
#include<conio.h>
int main(){
    char mn[80];
    gets(mn);                 // gets the whole string enter on screen
    getch();
    return(0);
}